__pycache__/
*.pyc
ha_widgets_config.json
desktop.ini

# Debug traces written when HA_WIDGET_DEBUG is set.
*_trace.txt
backdrop_err.txt
run_log.txt

# The program's own log: what it was doing when it last went away, and
# every Python stack in it if the GUI thread ever stops answering.
widget.log
hang_report.txt

# Build output
build/
dist/
*.spec

# Pictures collected while designing this, and other scratch files: kept
# to hand, not part of the program.
reference/
*.bak
